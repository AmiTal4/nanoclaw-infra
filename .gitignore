# Terraform
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Variable files containing secrets
*.tfvars
*.tfvars.json
!terraform.tfvars.example

# CLI config
.terraformrc
terraform.rc

# Claude Code scratch worktrees (ephemeral, created by agent runs)
.claude/worktrees/

# Instance-only secret OCID file written by /setup-bitwarden (never commit)
scripts/bws-secret.ocid

# Python
__pycache__/
*.pyc
